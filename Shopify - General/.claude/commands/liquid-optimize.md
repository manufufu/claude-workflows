---
name: liquid-optimize
description: Identifies and fixes inefficient Liquid code in Shopify themes
---

Run the liquid-optimize skill. Identify files to optimize, then check for nested loops, expensive object usage (all_products), redundant assigns, render calls inside loops, missing limit parameters, and inefficient filter chains. Generate a report with before/after code examples ranked by impact (High/Medium/Low).
